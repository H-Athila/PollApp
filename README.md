# PollApp
simple Spring Boot web application where  users can register using a username, log in, and create polls with multiple options. Other users can view available polls and vote in them. The app stores data using Spring Data JPA and follows a clean controller-based structure without service implementations
